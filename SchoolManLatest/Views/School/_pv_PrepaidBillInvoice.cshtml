
@model TrackTap.Models.PrepaidInvoiceDataList
@{
    var student = new TrackTap.Data.Student(Model.StudentId);
    var studentData = student.GetStudentData();
    DateTime currentData = Model.CurrentDatetime;
}
<style>

</style>
<div class="modal-dialog modal-lg" id="printPopUp" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" id="close-print-pop" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            <h4 class="modal-title" id="myModalLabel">Print Data</h4>
        </div>
        <div class="modal-body" id="tbStudentPayment">
            <table style="width: 100%; margin-bottom: 20px;" border="0" cellpadding="0" cellspacing="0">
                <tr>
                    <td colspan="3" class="text-center" style="border-bottom: 3px solid #8a8787;">
                        @if (student.School.FilePath != null)
                        {
                            <div class="frmHeader" style="margin:auto; display:table;">
                                <img src="@student.School.FilePath" width="90" style="position: absolute;left: 12px; margin-right: 5px;" />
                                <div style="text-align:center;">
                                    <h1>@student.SchoolName</h1>
                                    <h4>@student.SchoolAddress</h4>
                                </div>
                            </div>
                        }
                        else
                        {
                            <div class="frmHeader" style="width:100%; text-align:center; margin:auto; display:table;">
                                <h1>@student.SchoolName</h1>
                                <h4>@student.SchoolAddress</h4>
                            </div>

                        }
                    </td>
                </tr>
                <tr>
                    <td style="text-align:center; padding-top: 10px;font-size: 20px;" colspan="3">
                        <label>FEE RECEIPT </label>
                    </td>

                </tr>
                <tr>
                    <td style="padding-top:15px;">
                        <label>Student Name : </label> @studentData.StundentName
                    </td>
                    <td style="padding-top:15px;">
                        <label>Class : </label> @studentData.ClassName / @studentData.DivisionName
                    </td>
                    <td style="float: right; padding-top:15px;">
                        @*<label>BillNo : </label> @paymentList.FirstOrDefault().BillNo / @currentData.Year*@
                        <label>BillNo : </label> NA
                    </td>
                </tr>
                <tr>
                    <td style="">
                        <label>Adm.No : </label> @studentData.StudentSpecialId
                    </td>
                    <td style="">
                        <label>Date : </label> @currentData.ToString("dd/MM/yyyy") <span style="margin-left:15px;">@currentData.ToString("hh:mm tt")</span>
                    </td>
                    <td style="float:right;">
                        <label>Contact : </label> @studentData.ContactNumber
                    </td>
                </tr>
            </table>
            <hr style="border-top: 3px solid #8a8787;" />
            <div><p style="text-align: center; font-size: 20px; font-weight: bold;">Fee Details</p></div>
            <table style="width: 100%;" border="1" cellpadding="0" cellspacing="0">
                <thead>
                    <tr>
                        <th style="padding: 5px; width:20px; ">
                            SlNo
                        </th>
                        <th style="padding: 5px; text-align: left;">
                            Fees Description
                        </th>
                        <th style="padding: 5px; text-align: center;">
                            Payable Amount
                        </th>
                        <th style="width:20px; padding: 5px; ">
                            Discount
                        </th>
                        <th style="padding: 5px; text-align: center;">
                            Paid Amount
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var fee in Model.data)
                    {
                       <tr>
                           <td>@fee.SlNo</td>
                           <td>@fee.FeeName (@String.Format("{0:y}", fee.DueDate))</td>
                           <td>@fee.ActualAmt</td>
                           <td>@fee.DiscountAmt</td>
                           <td>@fee.AfterDiscountAmt</td>
                       </tr>
                    }
                </tbody>
            </table>
            <div style="margin-top:20px; font-weight:bold;" id="amt-inwrds-main">
                <span>Received Rupees <span id="amt-inwords"></span> </span>
            </div>
            <br />
            <div class="row" style="margin-right:50px; font-weight: bold;">
                <span class="pull-right">Cashier </span>
            </div>
            @if (student.School.FooterMessage != null)
            {
                <br />
                <div class="row" style="text-align:center; font-weight: bold;">
                    <span>@student.School.FooterMessage </span>
                </div>
            }
        </div>
        <div class="form-group text-center">
            <button class="btn pay-now-btn" id="btn-Print-bill" onclick="printContent('tbStudentPayment')"><i class="fa fa-print"></i> Print</button>
        </div>
    </div>
</div>
}
<script>

    function printContent(el) {
        var restorepage = document.body.innerHTML;
        var printcontent = document.getElementById(el).innerHTML;
        document.body.innerHTML = printcontent;
        window.print();
        document.body.innerHTML = restorepage;
        var id =@Model.studentId;
        var win = window.open("about:blank", "_self"); 
        win.close();
    }

    $(document).on('click', '.ng-click', function () {
        var id = $(this).attr('data-id');
        window.location.href = '/School/BillingDetails/' + id;
    });

    $(document).ready(function () {
        var amount = $('#total-paid-amt-wr').attr('data-id');
        if (parseFloat(amount) > 0) {
            var amountData = test_skill(amount)
            $('#amt-inwords').html(amountData);
        }
        else {
            $('#amt-inwrds-main').html("Amount fully paid from previous balance");
        }


    });

    function test_skill(amountt) {
        var amount = "";
        var junkVal = amountt;
        junkVal = Math.floor(junkVal);
        var obStr = new String(junkVal);
        numReversed = obStr.split("");
        actnumber = numReversed.reverse();

        if (Number(junkVal) >= 0) {
        }
        else {
            alert('wrong Number cannot be converted');
            return amount;
        }
        if (Number(junkVal) == 0) {
        }
        if (actnumber.length > 9) {
            alert('Oops!!!! the Number is too big to covertes');
            return amount;
        }

        var iWords = ["Zero", " One", " Two", " Three", " Four", " Five", " Six", " Seven", " Eight", " Nine"];
        var ePlace = ['Ten', ' Eleven', ' Twelve', ' Thirteen', ' Fourteen', ' Fifteen', ' Sixteen', ' Seventeen', ' Eighteen', ' Nineteen'];
        var tensPlace = ['dummy', ' Ten', ' Twenty', ' Thirty', ' Forty', ' Fifty', ' Sixty', ' Seventy', ' Eighty', ' Ninety'];

        var iWordsLength = numReversed.length;
        var totalWords = "";
        var inWords = new Array();
        var finalWord = "";
        j = 0;
        for (i = 0; i < iWordsLength; i++) {
            switch (i) {
                case 0:
                    if (actnumber[i] == 0 || actnumber[i + 1] == 1) {
                        inWords[j] = '';
                    }
                    else {
                        inWords[j] = iWords[actnumber[i]];
                    }
                    inWords[j] = inWords[j] + ' Only';
                    break;
                case 1:
                    tens_complication();
                    break;
                case 2:
                    if (actnumber[i] == 0) {
                        inWords[j] = '';
                    }
                    else if (actnumber[i - 1] != 0 && actnumber[i - 2] != 0) {
                        inWords[j] = iWords[actnumber[i]] + ' Hundred and';
                    }
                    else {
                        inWords[j] = iWords[actnumber[i]] + ' Hundred';
                    }
                    break;
                case 3:
                    if (actnumber[i] == 0 || actnumber[i + 1] == 1) {
                        inWords[j] = '';
                    }
                    else {
                        inWords[j] = iWords[actnumber[i]];
                    }
                    if (actnumber[i + 1] != 0 || actnumber[i] > 0) {
                        inWords[j] = inWords[j] + " Thousand";
                    }
                    break;
                case 4:
                    tens_complication();
                    break;
                case 5:
                    if (actnumber[i] == 0 || actnumber[i + 1] == 1) {
                        inWords[j] = '';
                    }
                    else {
                        inWords[j] = iWords[actnumber[i]];
                    }
                    if (actnumber[i + 1] != 0 || actnumber[i] > 0) {
                        inWords[j] = inWords[j] + " Lakh";
                    }
                    break;
                case 6:
                    tens_complication();
                    break;
                case 7:
                    if (actnumber[i] == 0 || actnumber[i + 1] == 1) {
                        inWords[j] = '';
                    }
                    else {
                        inWords[j] = iWords[actnumber[i]];
                    }
                    inWords[j] = inWords[j] + " Crore";
                    break;
                case 8:
                    tens_complication();
                    break;
                default:
                    break;
            }
            j++;
        }

        function tens_complication() {
            if (actnumber[i] == 0) {
                inWords[j] = '';
            }
            else if (actnumber[i] == 1) {
                inWords[j] = ePlace[actnumber[i - 1]];
            }
            else {
                inWords[j] = tensPlace[actnumber[i]];
            }
        }
        inWords.reverse();
        for (i = 0; i < inWords.length; i++) {
            finalWord += inWords[i];
        }
        amount = finalWord;
        return amount;
    }
</script>
