
@model TrackTap.Models.AddDayBookModel
@{

}

@*@using (Ajax.BeginForm("SearchVoucher", "Journal", null, new AjaxOptions
{
    HttpMethod = "POST",
    OnSuccess = "SearchVoucherSuccess",
    OnFailure = "SearchVoucherFailed",
}, new { id = "form-login" }))
{*@
    <div class="col-md-4 col-md-offset-4 " role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel9">Search Voucher Number</h4>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    <label>Type</label>
                    @Html.EnumDropDownListFor(m => m.TypeId, "--Choose--", new { @class = "form-control ", @data_live_search = "true" ,id="daybook-type-id"})
                    @Html.ValidationMessageFor(m => m.TypeId)
                </div>
                <div class="form-group">
                    <label>Voucher No</label>
                    @Html.TextBoxFor(m => m.SearchVoucherNo, new { autocomplete = "off", placeholder = "Voucher No", @class = "form-control"  })
                    <span id="spn-error-voucher-number" data-val="0" data-myval="10" style="color:red;"></span>
                    @Html.ValidationMessageFor(z => z.SearchVoucherNo)
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn pay-now-btn" id="btn-voucher-search-dummy">Search</button>
                <button type="button" class="btn pay-now-btn isHidden" id="btn-voucher-search">Search</button>
            </div>
        </div>
    </div>
@*}*@